# Copy this file to .env and fill in your own RPC endpoint.
# The public devnet RPC is heavily rate-limited (429s) -- use a private
# endpoint (Helius, QuickNode, etc.) for anything beyond quick testing.

VITE_RPC_URL=https://api.devnet.solana.com
VITE_CLUSTER_LABEL=devnet
